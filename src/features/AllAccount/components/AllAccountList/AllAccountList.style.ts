import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  mainContainer: {
    marginTop: '12px',
    position: 'absolute',
    width: '100%',
  },

  divider: {
    height: '0.5px',
    backgroundColor: '#E8E8E8',
  },
});